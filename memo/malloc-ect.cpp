#include <iostream>

using std::cout, std::string, std::cin;

int main() {
    // int n;

    // cout << "Enter size of array\n";
    // cin >> n;
    // int *a = (int *)malloc(n*sizeof(int));

    // for (int i= 0; i < n; i++){
    //     *(a + i) = i + 1;
    // }
    // int *b = (int*)realloc(a, 2*n*sizeof(int));
    // cout << "before address: " << a << "after adress: " << b;
    // // for (int i = 0; i < n; i++){
    // //     cout << a[i] << " ";
    // // }
    int m = 2;
    printf("0x%02x", m);

    return 0;
}